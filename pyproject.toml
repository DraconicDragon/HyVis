[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hyvis"
version = "0.9.0"
dependencies = [
    "requests",
    "hydrus-api",
    "vibe @ git+https://github.com/DraconicDragon/vibe@f1e2371",
]

[project.scripts]
hyvis = "hyvis.main:cli"

[tool.hatch.metadata]
allow-direct-references = true
