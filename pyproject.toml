[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hyvis"
version = "0.27.3"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "hydrus-api",
    "vibe @ git+https://github.com/DraconicDragon/vibe@e0fa3d016badb52758ff93a433466cd332f4d618",
    "tomli-w>=1.2.0",
    "pydantic>=2.13.4",
]

[project.scripts]
hyvis = "hyvis.main:cli"
hyvis-push-pending = "hyvis.push_pending:cli"

[tool.hatch.metadata]
allow-direct-references = true
